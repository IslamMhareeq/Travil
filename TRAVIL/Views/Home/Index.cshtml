@{
    ViewData["Title"] = "Home";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TRAVIL - Premium Travel Experiences</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --color-midnight: #0d1117;
            --color-navy: #161b22;
            --color-charcoal: #21262d;
            --color-slate: #30363d;
            --color-steel: #484f58;
            --color-silver: #8b949e;
            --color-white: #f0f6fc;
            --color-gold: #d4a853;
            --color-gold-muted: rgba(212, 168, 83, 0.3);
            --font-display: 'Playfair Display', serif;
            --font-body: 'Inter', sans-serif;
            --radius-md: 8px;
            --radius-lg: 12px;
            --radius-full: 9999px;
            --space-2: 8px;
            --space-3: 12px;
            --space-4: 16px;
            --space-5: 24px;
            --space-6: 32px;
            --space-7: 48px;
            --space-8: 64px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-body);
            background: var(--color-midnight);
            color: var(--color-white);
            line-height: 1.6;
        }

        /* Navbar */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: var(--space-4) var(--space-6);
            background: rgba(13, 17, 23, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--color-slate);
        }

            .navbar .container {
                max-width: 1400px;
                margin: 0 auto;
                display: flex;
                align-items: center;
                justify-content: space-between;
            }

        .nav-brand {
            display: flex;
            align-items: center;
            gap: var(--space-3);
            text-decoration: none;
        }

        .nav-logo {
            width: 44px;
            height: 44px;
            background: linear-gradient(135deg, var(--color-gold), #b8943f);
            border-radius: var(--radius-md);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            color: var(--color-midnight);
        }

        .nav-brand span {
            font-family: var(--font-display);
            font-size: 1.5rem;
            font-weight: 500;
            color: var(--color-white);
            letter-spacing: 0.1em;
        }

        .nav-links {
            display: flex;
            gap: var(--space-6);
        }

            .nav-links a {
                color: var(--color-silver);
                text-decoration: none;
                font-size: 0.9375rem;
                font-weight: 500;
                transition: color 0.2s;
            }

                .nav-links a:hover, .nav-links a.active {
                    color: var(--color-gold);
                }

        .nav-auth {
            display: flex;
            gap: var(--space-3);
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: var(--space-2);
            padding: var(--space-3) var(--space-5);
            font-size: 0.9375rem;
            font-weight: 500;
            text-decoration: none;
            border-radius: var(--radius-md);
            border: none;
            cursor: pointer;
            transition: all 0.2s;
        }

        .btn-ghost {
            background: transparent;
            color: var(--color-silver);
        }

            .btn-ghost:hover {
                color: var(--color-gold);
            }

        .btn-primary {
            background: linear-gradient(135deg, var(--color-gold), #b8943f);
            color: var(--color-midnight);
        }

            .btn-primary:hover {
                transform: translateY(-2px);
                box-shadow: 0 4px 20px rgba(212, 168, 83, 0.3);
            }

        .btn-outline {
            background: transparent;
            border: 1px solid var(--color-slate);
            color: var(--color-white);
        }

            .btn-outline:hover {
                border-color: var(--color-gold);
                color: var(--color-gold);
            }

        .btn-sm {
            padding: var(--space-2) var(--space-4);
            font-size: 0.875rem;
        }

        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            color: var(--color-white);
            font-size: 1.5rem;
            cursor: pointer;
        }

        /* Hero */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(rgba(13,17,23,0.7), rgba(13,17,23,0.9)), url('https://images.unsplash.com/photo-1506929562872-bb421503ef21?w=1920&q=80') center/cover;
            text-align: center;
            padding: 120px var(--space-6) var(--space-8);
        }

        .hero-content {
            max-width: 900px;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: var(--space-2);
            padding: var(--space-2) var(--space-4);
            background: var(--color-gold-muted);
            border: 1px solid var(--color-gold);
            border-radius: var(--radius-full);
            font-size: 0.8125rem;
            color: var(--color-gold);
            margin-bottom: var(--space-5);
        }

        .hero h1 {
            font-family: var(--font-display);
            font-size: 4rem;
            font-weight: 500;
            line-height: 1.1;
            margin-bottom: var(--space-5);
        }

        .hero p {
            font-size: 1.25rem;
            color: var(--color-silver);
            max-width: 600px;
            margin: 0 auto var(--space-6);
        }

        .hero-actions {
            display: flex;
            gap: var(--space-4);
            justify-content: center;
            flex-wrap: wrap;
        }

        .hero-stats {
            display: flex;
            gap: var(--space-8);
            justify-content: center;
            margin-top: var(--space-7);
            padding-top: var(--space-6);
            border-top: 1px solid var(--color-slate);
        }

        .hero-stat {
            text-align: center;
        }

        .hero-stat-value {
            font-family: var(--font-display);
            font-size: 2.5rem;
            font-weight: 500;
            color: var(--color-gold);
        }

        .hero-stat-label {
            font-size: 0.875rem;
            color: var(--color-silver);
        }

        /* Section */
        .section {
            padding: var(--space-8) var(--space-6);
        }

            .section .container {
                max-width: 1400px;
                margin: 0 auto;
            }

        .section-header {
            text-align: center;
            margin-bottom: var(--space-7);
        }

            .section-header h2 {
                font-family: var(--font-display);
                font-size: 2.5rem;
                font-weight: 500;
                margin-bottom: var(--space-3);
            }

            .section-header p {
                color: var(--color-silver);
                font-size: 1.125rem;
            }

        /* Destinations Grid */
        .destinations-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: var(--space-5);
        }

        .destination-card {
            position: relative;
            height: 320px;
            border-radius: var(--radius-lg);
            overflow: hidden;
            cursor: pointer;
        }

            .destination-card img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                transition: transform 0.5s;
            }

            .destination-card:hover img {
                transform: scale(1.1);
            }

            .destination-card::after {
                content: '';
                position: absolute;
                inset: 0;
                background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
            }

        .destination-card-content {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: var(--space-5);
            z-index: 1;
        }

        .destination-card h3 {
            font-family: var(--font-display);
            font-size: 1.5rem;
            margin-bottom: 4px;
        }

        .destination-card p {
            color: var(--color-silver);
            font-size: 0.875rem;
        }

        .destination-card .price {
            position: absolute;
            top: var(--space-4);
            right: var(--space-4);
            padding: var(--space-2) var(--space-4);
            background: var(--color-gold);
            border-radius: var(--radius-full);
            font-weight: 700;
            color: var(--color-midnight);
            font-size: 0.875rem;
            z-index: 2;
        }

        /* Packages Grid */
        .packages-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: var(--space-5);
        }

        .package-card {
            background: var(--color-navy);
            border: 1px solid var(--color-slate);
            border-radius: var(--radius-lg);
            overflow: hidden;
            transition: all 0.3s;
        }

            .package-card:hover {
                transform: translateY(-8px);
                border-color: var(--color-gold);
                box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            }

        .package-card-image {
            position: relative;
            height: 200px;
            overflow: hidden;
        }

            .package-card-image img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                transition: transform 0.3s;
            }

        .package-card:hover .package-card-image img {
            transform: scale(1.05);
        }

        .package-card-badge {
            position: absolute;
            top: var(--space-4);
            left: var(--space-4);
            padding: 4px 12px;
            background: var(--color-gold);
            color: var(--color-midnight);
            font-size: 0.75rem;
            font-weight: 600;
            border-radius: var(--radius-full);
        }

        .package-card-content {
            padding: var(--space-5);
        }

        .package-card-location {
            display: flex;
            align-items: center;
            gap: var(--space-2);
            color: var(--color-gold);
            font-size: 0.8125rem;
            margin-bottom: var(--space-2);
        }

        .package-card-content h3 {
            font-family: var(--font-display);
            font-size: 1.375rem;
            margin-bottom: var(--space-3);
        }

        .package-card-meta {
            display: flex;
            gap: var(--space-4);
            margin-bottom: var(--space-4);
        }

            .package-card-meta span {
                display: flex;
                align-items: center;
                gap: var(--space-2);
                font-size: 0.8125rem;
                color: var(--color-silver);
            }

        .package-card-footer {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding-top: var(--space-4);
            border-top: 1px solid var(--color-slate);
        }

        .package-card-price {
            font-family: var(--font-display);
            font-size: 1.375rem;
            font-weight: 600;
            color: var(--color-gold);
        }

            .package-card-price span {
                font-size: 0.875rem;
                color: var(--color-silver);
                font-weight: 400;
            }

            .package-card-price .old-price {
                text-decoration: line-through;
                color: var(--color-steel);
                font-size: 0.9rem;
                margin-right: var(--space-2);
            }

        /* Reviews Section */
        .reviews-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: var(--space-5);
        }

        .review-card {
            background: var(--color-navy);
            border: 1px solid var(--color-slate);
            border-radius: var(--radius-lg);
            padding: var(--space-6);
            transition: all 0.3s;
            position: relative;
        }

            .review-card:hover {
                border-color: var(--color-gold);
                transform: translateY(-4px);
            }

            .review-card::before {
                content: '"';
                position: absolute;
                top: 20px;
                right: 24px;
                font-family: var(--font-display);
                font-size: 5rem;
                color: var(--color-gold-muted);
                line-height: 1;
            }

        .review-stars {
            display: flex;
            gap: 4px;
            margin-bottom: var(--space-4);
        }

            .review-stars i {
                color: var(--color-gold);
                font-size: 1rem;
            }

                .review-stars i.empty {
                    color: var(--color-slate);
                }

        .review-text {
            color: var(--color-silver);
            font-size: 0.9375rem;
            line-height: 1.7;
            margin-bottom: var(--space-5);
            min-height: 80px;
            display: -webkit-box;
            -webkit-line-clamp: 4;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .review-author {
            display: flex;
            align-items: center;
            gap: var(--space-3);
            padding-top: var(--space-4);
            border-top: 1px solid var(--color-slate);
        }

        .review-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--color-gold), #b8943f);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            color: var(--color-midnight);
            font-size: 1.125rem;
            flex-shrink: 0;
        }

        .review-author-info h4 {
            font-size: 0.9375rem;
            font-weight: 600;
            margin-bottom: 2px;
        }

        .review-author-info p {
            font-size: 0.8125rem;
            color: var(--color-steel);
        }

        .review-destination {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-size: 0.75rem;
            color: var(--color-gold);
            background: var(--color-gold-muted);
            padding: 4px 10px;
            border-radius: var(--radius-full);
            margin-top: 8px;
        }

        /* Features Grid */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: var(--space-5);
        }

        .feature-card {
            text-align: center;
            padding: var(--space-6);
            background: var(--color-navy);
            border: 1px solid var(--color-slate);
            border-radius: var(--radius-lg);
            transition: all 0.3s;
        }

            .feature-card:hover {
                border-color: var(--color-gold);
                transform: translateY(-4px);
            }

            .feature-card i {
                font-size: 2.5rem;
                color: var(--color-gold);
                margin-bottom: var(--space-4);
            }

            .feature-card h3 {
                font-size: 1.125rem;
                margin-bottom: var(--space-3);
            }

            .feature-card p {
                font-size: 0.875rem;
                color: var(--color-silver);
            }

        /* Footer */
        .footer {
            background: var(--color-navy);
            border-top: 1px solid var(--color-slate);
            padding: var(--space-8) var(--space-6) var(--space-6);
        }

            .footer .container {
                max-width: 1400px;
                margin: 0 auto;
            }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: var(--space-6);
            margin-bottom: var(--space-6);
        }

        .footer-brand {
        }

        .footer-logo {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            margin-bottom: var(--space-4);
            text-decoration: none;
        }

        .footer-logo-icon {
            width: 44px;
            height: 44px;
            min-width: 44px;
            background: linear-gradient(135deg, var(--color-gold), #b8943f);
            border-radius: var(--radius-md);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            color: var(--color-midnight);
        }

        .footer-logo-text {
            font-family: var(--font-display);
            font-size: 1.5rem;
            font-weight: 500;
            color: var(--color-gold);
            letter-spacing: 0.1em;
            white-space: nowrap;
        }

        .footer-brand p {
            color: var(--color-silver);
            font-size: 0.9375rem;
            line-height: 1.7;
            margin-top: var(--space-3);
        }

        .footer-links h4 {
            font-size: 1rem;
            margin-bottom: var(--space-4);
            color: var(--color-white);
        }

        .footer-links a {
            display: flex;
            align-items: center;
            gap: var(--space-2);
            color: var(--color-silver);
            text-decoration: none;
            font-size: 0.9375rem;
            margin-bottom: var(--space-3);
            transition: color 0.2s;
        }

            .footer-links a:hover {
                color: var(--color-gold);
            }

        .footer-bottom {
            text-align: center;
            padding-top: var(--space-6);
            border-top: 1px solid var(--color-slate);
            color: var(--color-steel);
            font-size: 0.875rem;
        }

        /* Loading */
        .loading {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: var(--space-8);
            grid-column: 1 / -1;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 3px solid var(--color-slate);
            border-top-color: var(--color-gold);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @@keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        .empty-state {
            text-align: center;
            padding: var(--space-8);
            color: var(--color-silver);
            grid-column: 1 / -1;
        }

            .empty-state i {
                font-size: 3rem;
                margin-bottom: var(--space-4);
                color: var(--color-steel);
            }

        /* Responsive */
        @@media (max-width: 1024px) {
            .destinations-grid, .packages-grid, .reviews-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .features-grid, .footer-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @@media (max-width: 768px) {
            .nav-links, .nav-auth {
                display: none;
            }

            .mobile-menu-btn {
                display: block;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .hero-stats {
                flex-direction: column;
                gap: var(--space-4);
            }

            .destinations-grid, .packages-grid, .reviews-grid, .features-grid, .footer-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="nav-brand">
                <div class="nav-logo"><i class="fas fa-compass"></i></div>
                <span>TRAVIL</span>
            </a>
            <div class="nav-links" id="navLinks">
                <a href="/" class="active">Home</a>
                <a href="/packages">Packages</a>
                <a href="/destinations">Destinations</a>
                <a href="#about">About</a>
            </div>
            <div class="nav-auth" id="navAuth">
                <a href="/account/login" class="btn btn-ghost">Sign In</a>
                <a href="/account/register" class="btn btn-primary">Get Started</a>
            </div>
            <button class="mobile-menu-btn"><i class="fas fa-bars"></i></button>
        </div>
    </nav>

    <section class="hero">
        <div class="hero-content">
            <div class="hero-badge"><i class="fas fa-award"></i> Award Winning Travel Agency</div>
            <h1>Discover the World's Most <span style="color: var(--color-gold);">Extraordinary</span> Destinations</h1>
            <p>Curated luxury travel experiences that transform journeys into lifelong memories.</p>
            <div class="hero-actions">
                <a href="/packages" class="btn btn-primary"><i class="fas fa-compass"></i> Explore Packages</a>
                <a href="#about" class="btn btn-outline"><i class="fas fa-play"></i> Learn More</a>
            </div>
            <div class="hero-stats">
                <div class="hero-stat"><div class="hero-stat-value" id="totalPackages">--</div><div class="hero-stat-label">Travel Packages</div></div>
                <div class="hero-stat"><div class="hero-stat-value" id="totalDestinations">--</div><div class="hero-stat-label">Destinations</div></div>
                <div class="hero-stat"><div class="hero-stat-value" id="totalReviews">--</div><div class="hero-stat-label">Happy Travelers</div></div>
            </div>
        </div>
    </section>

    <section class="section" id="destinations">
        <div class="container">
            <div class="section-header">
                <h2>Popular Destinations</h2>
                <p>Explore our most sought-after travel destinations around the globe</p>
            </div>
            <div class="destinations-grid" id="destinationsGrid"><div class="loading"><div class="spinner"></div></div></div>
            <div style="text-align: center; margin-top: var(--space-6);">
                <a href="/destinations" class="btn btn-outline"><i class="fas fa-globe"></i> View All Destinations</a>
            </div>
        </div>
    </section>

    <section class="section" style="background: var(--color-navy);">
        <div class="container">
            <div class="section-header">
                <h2>Featured Packages</h2>
                <p>Curated travel experiences designed to create lasting memories</p>
            </div>
            <div class="packages-grid" id="packagesGrid"><div class="loading"><div class="spinner"></div></div></div>
            <div style="text-align: center; margin-top: var(--space-6);">
                <a href="/packages" class="btn btn-outline"><i class="fas fa-th-large"></i> View All Packages</a>
            </div>
        </div>
    </section>

    <!-- Reviews Section -->
    <section class="section" id="reviews">
        <div class="container">
            <div class="section-header">
                <h2>What Our Travelers Say</h2>
                <p>Real experiences from our valued customers</p>
            </div>
            <div class="reviews-grid" id="reviewsGrid"><div class="loading"><div class="spinner"></div></div></div>
        </div>
    </section>

    <section class="section" style="background: var(--color-navy);" id="about">
        <div class="container">
            <div class="section-header">
                <h2>Why Choose Us</h2>
                <p>Experience travel the way it was meant to be</p>
            </div>
            <div class="features-grid">
                <div class="feature-card"><i class="fas fa-shield-alt"></i><h3>Secure Booking</h3><p>Your payments and data protected with enterprise-grade security.</p></div>
                <div class="feature-card"><i class="fas fa-headset"></i><h3>24/7 Support</h3><p>Our dedicated team is available around the clock.</p></div>
                <div class="feature-card"><i class="fas fa-hand-holding-usd"></i><h3>Best Price Guarantee</h3><p>Price match guarantee on all bookings.</p></div>
                <div class="feature-card"><i class="fas fa-star"></i><h3>Curated Experiences</h3><p>Carefully crafted by travel experts.</p></div>
            </div>
        </div>
    </section>

    <footer class="footer" id="contact">
        <div class="container">
            <div class="footer-grid">
                <div class="footer-brand">
                    <a href="/" class="footer-logo">
                        <div class="footer-logo-icon"><i class="fas fa-compass"></i></div>
                        <span class="footer-logo-text">TRAVIL</span>
                    </a>
                    <p>Creating extraordinary travel experiences since 2020.</p>
                </div>
                <div class="footer-links">
                    <h4>Quick Links</h4>
                    <a href="/">Home</a>
                    <a href="/packages">Packages</a>
                    <a href="/destinations">Destinations</a>
                    <a href="/my-trips">My Trips</a>
                </div>
                <div class="footer-links">
                    <h4>Support</h4>
                    <a href="#">FAQ</a>
                    <a href="#">Contact Us</a>
                    <a href="#">Privacy Policy</a>
                </div>
                <div class="footer-links">
                    <h4>Contact</h4>
                    <a href="mailto:info@travil.com"><i class="fas fa-envelope"></i> info@travil.com</a>
                    <a href="tel:+1234567890"><i class="fas fa-phone"></i> +1 (234) 567-890</a>
                </div>
            </div>
            <div class="footer-bottom"><p>&copy; 2025 TRAVIL. All rights reserved.</p></div>
        </div>
    </footer>

    <script>
        var allPackages = [];

        function updateNav() {
            var token = localStorage.getItem('token');
            var user = localStorage.getItem('user');
            if (token && user) {
                try {
                    var userData = JSON.parse(user);
                    var isAdmin = userData.role === 0 || userData.role === '0' || userData.role === 'Admin';
                    document.getElementById('navLinks').innerHTML = '<a href="/" class="active">Home</a><a href="/packages">Packages</a><a href="/destinations">Destinations</a><a href="/my-trips">My Trips</a><a href="#about">About</a>';
                    document.getElementById('navAuth').innerHTML = '<a href="' + (isAdmin ? '/admin/dashboard' : '/account/dashboard') + '" class="btn btn-ghost"><i class="fas fa-user"></i> Dashboard</a><a href="#" class="btn btn-outline" onclick="logout(event)">Logout</a>';
                } catch(e) {}
            }
        }

        function logout(e) { e.preventDefault(); localStorage.removeItem('token'); localStorage.removeItem('user'); location.reload(); }

        function getImg(i) {
            var imgs=['https://images.unsplash.com/photo-1537996194471-e657df975ab4?w=800','https://images.unsplash.com/photo-1499856871958-5b9627545d1a?w=800','https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?w=800','https://images.unsplash.com/photo-1506929562872-bb421503ef21?w=800'];
            return imgs[i%imgs.length];
        }

        function renderStars(rating) {
            var html = '';
            for (var i = 1; i <= 5; i++) {
                html += '<i class="fas fa-star ' + (i <= rating ? '' : 'empty') + '"></i>';
            }
            return html;
        }

        function getInitials(name) {
            if (!name) return '?';
            var parts = name.split(' ');
            if (parts.length >= 2) {
                return (parts[0][0] + parts[1][0]).toUpperCase();
            }
            return name.substring(0, 2).toUpperCase();
        }

        function shuffleArray(array) {
            var shuffled = array.slice();
            for (var i = shuffled.length - 1; i > 0; i--) {
                var j = Math.floor(Math.random() * (i + 1));
                var temp = shuffled[i];
                shuffled[i] = shuffled[j];
                shuffled[j] = temp;
            }
            return shuffled;
        }

        async function loadReviews() {
            var reviewsGrid = document.getElementById('reviewsGrid');
            var allReviews = [];

            try {
                for (var i = 0; i < Math.min(allPackages.length, 10); i++) {
                    var pkg = allPackages[i];
                    try {
                        var response = await fetch('/api/review/package/' + pkg.packageId);
                        if (response.ok) {
                            var result = await response.json();
                            var reviews = result.data || result || [];
                            reviews.forEach(function(review) {
                                review.packageName = pkg.destination;
                                review.packageCountry = pkg.country;
                                allReviews.push(review);
                            });
                        }
                    } catch(e) {}
                }

                document.getElementById('totalReviews').textContent = (allReviews.length > 0 ? allReviews.length : '5K') + '+';

                if (allReviews.length === 0) {
                    reviewsGrid.innerHTML = getPlaceholderReviews();
                    return;
                }

                var randomReviews = shuffleArray(allReviews).slice(0, 3);

                var html = '';
                randomReviews.forEach(function(review) {
                    html += '<div class="review-card">';
                    html += '<div class="review-stars">' + renderStars(review.rating || 5) + '</div>';
                    html += '<p class="review-text">' + (review.comment || 'Amazing experience! Highly recommended.') + '</p>';
                    html += '<div class="review-author">';
                    html += '<div class="review-avatar">' + getInitials(review.userName || 'Guest') + '</div>';
                    html += '<div class="review-author-info">';
                    html += '<h4>' + (review.userName || 'Happy Traveler') + '</h4>';
                    html += '<p>Verified Traveler</p>';
                    if (review.packageName) {
                        html += '<span class="review-destination"><i class="fas fa-map-marker-alt"></i> ' + review.packageName + '</span>';
                    }
                    html += '</div></div></div>';
                });

                reviewsGrid.innerHTML = html;

            } catch (error) {
                console.error('Error loading reviews:', error);
                reviewsGrid.innerHTML = getPlaceholderReviews();
            }
        }

        function getPlaceholderReviews() {
            return '<div class="review-card"><div class="review-stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i></div><p class="review-text">An absolutely unforgettable experience! The attention to detail and personalized service made our honeymoon truly magical. Will definitely book again!</p><div class="review-author"><div class="review-avatar">JD</div><div class="review-author-info"><h4>John Davis</h4><p>Verified Traveler</p><span class="review-destination"><i class="fas fa-map-marker-alt"></i> Maldives</span></div></div></div><div class="review-card"><div class="review-stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i></div><p class="review-text">TRAVIL made planning our family vacation so easy. The kids loved every moment and we created memories that will last a lifetime. Highly recommend!</p><div class="review-author"><div class="review-avatar">SM</div><div class="review-author-info"><h4>Sarah Mitchell</h4><p>Verified Traveler</p><span class="review-destination"><i class="fas fa-map-marker-alt"></i> Bali</span></div></div></div><div class="review-card"><div class="review-stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star empty"></i></div><p class="review-text">The Swiss Alps adventure exceeded all expectations. Professional guides, stunning views, and seamless organization. Already planning my next trip!</p><div class="review-author"><div class="review-avatar">MR</div><div class="review-author-info"><h4>Michael Roberts</h4><p>Verified Traveler</p><span class="review-destination"><i class="fas fa-map-marker-alt"></i> Switzerland</span></div></div></div>';
        }

        async function loadPackages() {
            var packagesGrid = document.getElementById('packagesGrid');
            var destinationsGrid = document.getElementById('destinationsGrid');

            try {
                var r = await fetch('/api/travelpackage');
                if (r.ok) {
                    var d = await r.json();
                    allPackages = d.data || d || [];

                    document.getElementById('totalPackages').textContent = allPackages.length + '+';
                    var countries = {};
                    allPackages.forEach(function(p) { if (p.country) countries[p.country] = true; });
                    document.getElementById('totalDestinations').textContent = Object.keys(countries).length + '+';

                    if (allPackages.length > 0) {
                        var html = '';
                        allPackages.slice(0, 6).forEach(function(p, i) {
                            var dur = p.startDate && p.endDate ? Math.ceil((new Date(p.endDate) - new Date(p.startDate)) / 86400000) : 7;
                            var disc = p.discountedPrice && p.discountedPrice < p.price;
                            var price = disc ? p.discountedPrice : p.price;
                            html += '<div class="package-card"><div class="package-card-image"><img src="'+(p.imageUrl||getImg(i))+'" onerror="this.src=\''+getImg(i)+'\'">';
                            if (disc) html += '<span class="package-card-badge">Sale</span>';
                            html += '</div><div class="package-card-content"><div class="package-card-location"><i class="fas fa-map-marker-alt"></i> '+(p.country||'Unknown')+'</div><h3>'+(p.destination||'Package')+'</h3><div class="package-card-meta"><span><i class="far fa-clock"></i> '+dur+' Days</span><span><i class="fas fa-bed"></i> '+(p.availableRooms||0)+' Rooms</span></div><div class="package-card-footer"><div class="package-card-price">';
                            if (disc) html += '<span class="old-price">$'+Math.round(p.price)+'</span>';
                            html += '$'+Math.round(price)+' <span>/ person</span></div><a href="/packages/'+p.packageId+'" class="btn btn-primary btn-sm">View</a></div></div></div>';
                        });
                        packagesGrid.innerHTML = html;

                        var ctrs = {};
                        allPackages.forEach(function(p, i) {
                            if (p.country && !ctrs[p.country]) ctrs[p.country] = { country: p.country, destination: p.destination, img: p.imageUrl||getImg(i), price: p.discountedPrice||p.price, count: 1 };
                            else if (p.country) { ctrs[p.country].count++; if ((p.discountedPrice||p.price) < ctrs[p.country].price) ctrs[p.country].price = p.discountedPrice||p.price; }
                        });
                        var dh = '';
                        Object.values(ctrs).slice(0,4).forEach(function(d, i) {
                            dh += '<div class="destination-card" onclick="location.href=\'/destinations\'"><img src="'+d.img+'" onerror="this.src=\''+getImg(i)+'\'"><span class="price">From $'+Math.round(d.price)+'</span><div class="destination-card-content"><h3>'+d.destination+', '+d.country+'</h3><p>'+d.count+' Package'+(d.count>1?'s':'')+'</p></div></div>';
                        });
                        destinationsGrid.innerHTML = dh || '<div class="empty-state"><i class="fas fa-globe"></i><p>Coming soon</p></div>';

                        loadReviews();
                    }
                }
            } catch(e) {
                console.error(e);
                document.getElementById('reviewsGrid').innerHTML = getPlaceholderReviews();
            }
        }

        document.addEventListener('DOMContentLoaded', function() {
            updateNav();
            loadPackages();
        });
    </script>
</body>
</html>
